<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.msy.rrodemo.ui.MainActivity">
    <RadioGroup
        android:id="@+id/bottom_group"
        android:layout_width="match_parent"
        android:layout_height="@dimen/default_height"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:background="@color/default_backgroud">
        <RadioButton
            android:id="@+id/home_btn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:text="首页"
            android:textSize="@dimen/second_content_text_size"
            android:drawableTop="@drawable/main_home_selector_bg"
            android:textColor="@drawable/main_bottom_text_selector_bg"
            android:paddingTop="6dp"
            android:gravity="center"/>
        <RadioButton
            android:id="@+id/product_btn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:text="产品"
            android:textSize="@dimen/second_content_text_size"
            android:drawableTop="@drawable/main_product_selector_bg"
            android:textColor="@drawable/main_bottom_text_selector_bg"
            android:paddingTop="6dp"
            android:gravity="center"/>
        <RadioButton
            android:id="@+id/contacts_btn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:text="会话"
            android:textSize="@dimen/second_content_text_size"
            android:drawableTop="@drawable/main_contacts_selector_bg"
            android:textColor="@drawable/main_bottom_text_selector_bg"
            android:paddingTop="6dp"
            android:gravity="center"/>
        <RadioButton
            android:id="@+id/mine_btn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:text="我的"
            android:textSize="@dimen/second_content_text_size"
            android:drawableTop="@drawable/main_mine_selector_bg"
            android:textColor="@drawable/main_bottom_text_selector_bg"
            android:paddingTop="6dp"
            android:gravity="center"/>
    </RadioGroup>
    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_group"
        android:background="@color/white"/>
</RelativeLayout>
